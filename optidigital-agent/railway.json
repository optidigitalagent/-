{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "PLAYWRIGHT_BROWSERS_PATH=0 python -m playwright install chromium --with-deps"
  },
  "deploy": {
    "startCommand": "python bot/main.py"
  }
}
